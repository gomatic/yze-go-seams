// Package tested reaches for the real world only from its test file, which is
// out of scope: what a test does with a real resource is the test's business.
package tested

// Name is the value the package's test reads back.
const Name = "tested"
